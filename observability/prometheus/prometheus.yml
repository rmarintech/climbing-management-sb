global:
  # every 5s --> Prometheus --> GET /actuator/prometheus
  scrape_interval: 5s

scrape_configs:

  - job_name: "enrollment-service"

    metrics_path: "/actuator/prometheus"

    static_configs:
      - targets:
          - "host.docker.internal:8081"